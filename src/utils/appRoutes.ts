const APP_ROUTES = {
  auth: {
    login: '/auth/login',
  },
  tabs: {
    services: '/(tabs)/services',
    providers: '/(tabs)/providers',
    buildings: '/(tabs)/buildings',
  },
};

export default APP_ROUTES;
